import { createStyles } from '@mantine/core';

export const useStyles = createStyles((theme, _params, getRef) => ({
    container: {
        border: 'solid red',
        width: '100%',
        height: '100vh',
        display: 'flex',
    },

}));
